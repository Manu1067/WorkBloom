I am continuing my existing WorkBloom Spring Boot backend project.

IMPORTANT:
Do NOT start the project from scratch.
Do NOT redesign the existing architecture.
Do NOT rewrite completed modules unnecessarily.

First inspect the ENTIRE existing backend codebase and understand what has already been implemented.

WorkBloom is an employee experience, well-being, community, and engagement platform built around the vision:

"A platform that makes workplace feel like home."

We have already completed several modules and features. Use the existing completed code as the SOURCE OF TRUTH for architecture, naming conventions, coding style, DTO patterns, entity relationships, repository patterns, service interfaces, serviceImpl implementations, controllers, exception handling, validation, and security.

The remaining work is:

1. Travel REFACTORING
2. Event
3. Community
4. Dashboard
5. Chat
6. Club

==================================================
GENERAL CODING RULES
==================================================

For all remaining work, follow the SAME architecture and coding pattern already used in the completed WorkBloom modules.

The standard structure is:

module/
├── controller/
├── dto/
├── entity/
├── repository/
├── service/
└── serviceImpl/

Where appropriate, implement:

Entity
→ Enum / related entity
→ Repository
→ Request DTO
→ Response DTO
→ Service
→ ServiceImpl
→ Controller
→ Integration
→ Postman testing

Use the existing WorkBloom code as the reference rather than inventing a new architecture.

IMPORTANT:

- Do NOT create duplicate classes.
- Do NOT remove working functionality.
- Do NOT rewrite completed modules unnecessarily.
- Do NOT rename existing classes unless absolutely necessary.
- Do NOT introduce a completely different coding style.
- Reuse existing BaseEntity where appropriate.
- Reuse existing security/authentication mechanisms.
- Reuse existing exception handling.
- Reuse existing common utilities.
- Keep controllers thin.
- Keep business logic inside ServiceImpl.
- Keep database operations inside repositories.
- Use DTOs instead of exposing entities directly through APIs.
- Maintain proper relationships between entities.
- Maintain proper role and permission handling.
- Keep the code understandable and maintainable.
- Do not turn WorkBloom into a collection of unrelated CRUD examples.

==================================================
VERY IMPORTANT — CODE OUTPUT
==================================================

I DO NOT want ZIP files.

I DO NOT want only a list of files to create.

I DO NOT want instructions such as:

"Create EventRepository.java."

Instead, PROVIDE THE COMPLETE CODE for every new or modified class.

For example:

EventRepository.java
→ complete package declaration
→ complete imports
→ complete interface
→ complete methods

EventService.java
→ complete code

EventServiceImpl.java
→ complete code

EventController.java
→ complete code

Do this for every required class.

The code should be directly usable in the existing project.

==================================================
1. TRAVEL REFACTORING
==================================================

There are Travel-related files/functionality currently associated with the Wellness module.

The standalone Travel module exists but needs to be properly separated/refactored.

DO NOT build Travel from scratch.

First inspect the Wellness module and identify all classes that are genuinely Travel-specific, including:

- Travel entities
- Travel DTOs
- Travel repositories
- Travel services
- Travel serviceImpl classes
- Travel controllers
- Travel enums
- Travel-specific supporting classes

Move ONLY the genuinely Travel-specific functionality into the standalone Travel module.

The standalone structure should follow:

travel/
├── controller/
├── dto/
├── entity/
├── repository/
├── service/
└── serviceImpl/

IMPORTANT:

- Do NOT simply duplicate the Travel files.
- Actually refactor/move the appropriate classes.
- Update package declarations.
- Update imports.
- Update references throughout the project.
- Update service dependencies.
- Update controller references if required.
- Remove obsolete duplicates from Wellness.
- Do NOT break Wellness functionality.
- Keep Wellness-specific mood and well-being functionality inside Wellness.
- Keep Travel-specific functionality inside Travel.
- If Wellness needs Travel functionality, create a clean dependency between the modules rather than keeping Travel classes inside Wellness.
- Preserve existing business logic.
- Do not unnecessarily change API behavior.
- Compile the project after the refactoring.
- Fix all package/import/reference errors.
- Test affected Wellness and Travel APIs through Postman.

This is a REFACTORING task, not a new Travel implementation.

==================================================
2. EVENT MODULE
==================================================

WorkBloom Events is an employee engagement and social activity system, not just basic CRUD.

Employees should be able to discover and participate in:

- Music nights
- Gaming sessions
- Movie nights
- Coffee chats
- Sports activities
- Hobby sessions
- Cultural celebrations
- Team outings
- Wellness activities
- Talent-sharing sessions
- Other company/social activities

Authorized users such as HR, Admin, or authorized event organizers should be able to:

- Create events
- Update events
- Cancel events
- Manage event capacity
- View registered employees
- Manage their own events according to permissions

Employees should be able to:

- View upcoming events
- View event details
- Discover events
- Filter events
- Register for events
- Cancel their registration

The system should prevent:

- Duplicate registration
- Registration when capacity is full
- Registration after the event has ended
- Unauthorized modification of another user's event

Events should contain appropriate information such as:

- title
- description
- event type
- date
- start time
- end time
- location
- capacity
- organizer
- banner/image where appropriate
- status
- audit information

Use a separate EventRegistration relationship/entity rather than storing registrations directly inside Event.

Event types may include:

- MUSIC
- GAMING
- SPORTS
- HOBBY
- SOCIAL
- CULTURAL
- TEAM_OUTING
- WELLNESS
- LEARNING
- OTHER

Integrate with the existing Notification module where appropriate.

Examples:

Employee registers:
→ notification confirming registration

Event is updated:
→ notify registered employees

Event is cancelled:
→ notify registered employees

Upcoming event:
→ reminder notification where appropriate

We will use n8n for automated workflows/reminders later, so keep the backend compatible with n8n.

Do not replace normal Spring Boot business logic with n8n.

==================================================
3. COMMUNITY MODULE
==================================================

The Community module is a core WorkBloom feature for employee interaction and workplace connection.

Employees should be able to:

- Create community posts
- View a community feed
- View post details
- Like/react to posts
- Comment on posts
- Edit their own posts
- Delete their own posts
- Edit their own comments
- Delete their own comments
- View post author information
- View timestamps
- View engagement counts
- Discover recent/popular posts where appropriate

Posts can represent:

- Workplace updates
- Achievements
- Team memories
- Event updates
- Event photos
- Hobby discussions
- Learning discussions
- Social discussions
- Appreciation
- Positive workplace moments
- General employee community content

Use proper relationships for:

- CommunityPost
- Comment
- Reaction/Like
- Employee/User

Business rules:

- Users can edit/delete their own posts.
- HR/Admin may have moderation privileges where appropriate.
- Users can edit/delete their own comments.
- Users should not be able to like/react to the same post multiple times.
- Ownership must be validated.
- Unauthorized access must be prevented.
- Sensitive employee information must not be exposed.
- Use appropriate deletion/moderation handling consistent with the existing project.

Integrate with Notification where appropriate.

Examples:

Someone comments on your post
→ notification

Someone reacts to your post
→ notification

Important community announcement
→ notification where appropriate

Keep Community compatible with future n8n workflows.

==================================================
4. DASHBOARD MODULE
==================================================

The Dashboard should aggregate useful information from the existing WorkBloom modules.

Do NOT create duplicate data just for Dashboard.

Use existing services/repositories where appropriate.

EMPLOYEE DASHBOARD may contain:

- Employee profile summary
- Upcoming registered events
- Recent notifications
- Recognition received
- Wellness/mood summary where appropriate
- Buddy information
- Learning progress
- Community activity
- Impact/volunteering participation
- Other useful personalized WorkBloom information

HR/ADMIN DASHBOARD may contain appropriate organization-level statistics such as:

- Employee statistics
- Event participation
- Recognition activity
- Community engagement
- Wellness statistics
- Learning participation
- Impact/volunteering statistics
- Other useful organizational metrics

IMPORTANT:

Respect employee privacy.

Do NOT expose sensitive information such as salary to ordinary employees.

Dashboard should aggregate data from existing modules instead of duplicating business logic.

==================================================
5. CHAT MODULE
==================================================

Chat is WorkBloom's employee communication system.

Employees should be able to:

- Start conversations
- View conversations
- Send messages
- View message history
- Track message timestamps
- Identify read/unread messages
- Participate in employee-to-employee conversations

The architecture should be capable of supporting future group conversations if appropriate.

Use proper concepts such as:

- Conversation
- ConversationParticipant
- Message
- MessageStatus / ReadStatus where appropriate

Business rules:

- Users can only access conversations they participate in.
- Users cannot read another user's private conversation.
- Users cannot send messages into conversations they are not authorized to access.
- Message ownership should be validated.
- Integrate properly with existing Employee/Auth/Security modules.

Keep the backend REST-based according to the current project.

Make it possible to introduce WebSocket/real-time messaging later without unnecessarily complicating the current implementation.

==================================================
6. CLUB MODULE
==================================================

The Club module supports employee interest and hobby communities.

Employees should be able to:

- Discover clubs
- View club details
- Join clubs
- Leave clubs
- View club members
- Participate in club activities where appropriate

Examples:

- Music Club
- Gaming Club
- Sports Club
- Photography Club
- Book Club
- Coding Club
- Movie Club
- Fitness Club
- Travel Club
- Cultural Club
- Other employee-interest clubs

Authorized club creators/admins should be able to:

- Create clubs
- Update club information
- Manage club membership
- Remove members where authorized
- Manage club status

Use a proper ClubMembership relationship/entity rather than storing members as a simple list/string.

Clubs can optionally be associated with Events where appropriate.

For example:

Club
→ organizes
→ Event

But avoid unnecessary tight coupling between modules.

Follow the same WorkBloom architecture:

club/
├── controller/
├── dto/
├── entity/
├── repository/
├── service/
└── serviceImpl/

==================================================
SECURITY AND DATA PRIVACY
==================================================

Maintain the security principles already established in WorkBloom.

Different users have different permissions.

For example:

- Employee can access their own private information.
- HR/Admin can manage authorized employee information.
- Salary must NOT be exposed to ordinary employees.
- Employees must not be allowed to modify HR-controlled information.
- Users cannot access another user's private chat.
- Users cannot modify another user's community content without appropriate permissions.
- Event organizers can manage their own events.
- Club administrators can manage their clubs.

Use the existing authentication/security implementation instead of creating a second authentication system.

==================================================
TESTING WORKFLOW
==================================================

We are testing WorkBloom properly using:

Spring Boot
+
PostgreSQL
+
Postman

For every module:

1. Implement the code.
2. Compile/run the Spring Boot backend.
3. Fix compilation errors.
4. Fix runtime errors.
5. Verify database relationships.
6. Test APIs using Postman.
7. Test success cases.
8. Test validation failures.
9. Test unauthorized/forbidden cases where applicable.
10. Verify the module before moving to the next module.

For each completed module, provide the Postman testing flow including:

- HTTP method
- Endpoint
- Required headers
- Request body
- Expected response
- Important negative test cases

==================================================
N8N
==================================================

WorkBloom will use n8n for workflow automation and integrations.

Potential workflows include:

- Event reminders
- Notification automation
- Wellness reminders
- Scheduled employee reminders
- Community-related workflows
- Other triggered/scheduled workflows

Do not move core business logic from Spring Boot into n8n.

Spring Boot remains responsible for the main application/business logic.

n8n should be used for automation and integration.

==================================================
IMPLEMENTATION ORDER
==================================================

Work in this order:

1. Travel refactoring
2. Event
3. Community
4. Dashboard
5. Chat
6. Club

Do ONE module at a time.

Do not generate everything as one huge uncontrolled change.

For each module:

- Inspect existing code first.
- Preserve existing functionality.
- Identify missing functionality.
- Implement it.
- Provide complete code for new/modified classes.
- Explain important design decisions.
- Compile/test.
- Give Postman testing details.
- Then move to the next module.

==================================================
FINAL REQUIREMENT
==================================================

The goal is to CONTINUE WORKBLOOM FROM WHERE WE LEFT OFF.

This is an existing project with substantial completed work.

Make the remaining modules look like they were developed by the SAME developer using the SAME architecture and coding conventions.

Do not create generic tutorial implementations.

Do not restart.

Do not redesign.

Do not generate ZIP files.

Give COMPLETE CODE directly for the required classes and make the changes directly in the existing project.

Start by inspecting the current project and then begin with:

TRAVEL REFACTORING

After Travel refactoring is verified, continue with Event → Community → Dashboard → Chat → Club.